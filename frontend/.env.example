# URL of the token-api service, reachable from the BROWSER (not from inside
# the frontend container) since the fetch happens client-side.
# Local dev with docker-compose: token-api is published on host port 8081.
VITE_TOKEN_ENDPOINT=http://localhost:8081/token
